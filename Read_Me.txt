1. To start the project open command promt in current(project) directory and enter below command:
   'python app.py'
   
   After this go to browser and enter the address http://127.0.0.1:5000

2. The dataset file is 'world_population_data.csv'.